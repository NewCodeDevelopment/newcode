{
	"$schema": "https://turborepo.org/schema.json",
	"pipeline": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".next/**"],
			"env": ["NODE_ENV"]
		},
		"lint": {
			"outputs": []
		},
		"dev": {
			"cache": false,
			"env": ["NODE_ENV"]
		}
	},
	"globalEnv": ["NODE_ENV"]
}
